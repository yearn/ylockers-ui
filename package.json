{
  "name": "ylockers-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev:prisma": "cd packages/prisma && next dev",
    "dev:crv": "cd packages/crv && next dev",
    "clean": "rm -R node_modules && rm -R packages/*/.next"
  }
}
